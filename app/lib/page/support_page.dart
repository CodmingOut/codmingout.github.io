import 'package:app/widget/upper_bar_widget.dart';
import 'package:flutter/material.dart';

class SupportPage extends StatelessWidget {
  static final String routeName = '/support';

  @override
  Widget build(BuildContext context) {
    return UpperBarWidget(
      child: Container()
    );
  }
}